# おにぎりカレンダー UIリデザイン計画

## 概要

現在のUIはシンプルだが、モバイルでの使い勝手に課題がある。モバイルファーストのシンプル/クリーンなデザインに改善し、おにぎりの登録・閲覧体験を向上させる。

---

## 現状の問題点

### カレンダー画面
- モバイルではセルが80pxと小さく、おにぎり画像が非表示
- 1セルに1つのおにぎり名のみで情報量不足
- 月ナビゲーションが小さい矢印ボタンのみでタップしづらい
- スワイプ操作に非対応

### 登録/詳細ダイアログ
- 中央モーダルでモバイルでの操作感が悪い
- フォームに視覚的なグルーピングがない
- 星評価がテキスト文字（★）で味気ない
- 画像アップロードUIが分かりにくい
- 詳細表示が地味でおにぎりの魅力が伝わらない

### 全体
- 空状態のガイダンスがない
- ヘッダーにアプリのブランド感が薄い
- 保存フィードバックがalertのみ

---

## デザイン方針

| 項目 | 方針 |
|------|------|
| ターゲットデバイス | モバイルメイン |
| デザインテイスト | シンプル/クリーン（白基調、余計な装飾なし） |
| ダイアログ形式 | ボトムシート（下からスライドアップ） |
| カレンダーセル | サムネイル画像を大きく表示 |
| 月ナビゲーション | スワイプ操作対応 |
| フォーム | 1ページ構成のまま洗練 |
| 検索 | 現状のタブ切り替えを改善 |

---

## フェーズ1: カレンダー画面の改善

### 1-1. カレンダーセルのサムネイル拡大

**対象ファイル:** `app/components/calendar/calendar-grid.tsx`

**現状:**
- セルサイズ: `h-20 sm:h-32`（80px / 128px）
- おにぎり画像: モバイルでは `hidden sm:block` で非表示
- テキスト: おにぎり名のみ小さく表示

**変更内容:**
- セルサイズを拡大: `h-24 sm:h-36`（96px / 144px）
- モバイルでもサムネイル画像を表示（`hidden` を削除）
- 画像をセルの背景として表示し、日付番号をオーバーレイ
- おにぎりがない日はプレーンなセル
- おにぎりがある日は画像が目立つデザイン

**イメージ:**
```
┌─────────┐  ┌─────────┐
│ 1       │  │ 2       │
│         │  │ [IMAGE] │  ← おにぎり画像をセル内に大きく表示
│         │  │ 鮭おに  │
└─────────┘  └─────────┘
```

### 1-2. スワイプナビゲーション

**対象ファイル:** `app/components/calendar/calendar-grid.tsx`

**変更内容:**
- タッチイベント（`touchstart` / `touchend`）でスワイプ検出
- 左スワイプ → 次月、右スワイプ → 前月
- スワイプ閾値: 50px以上の水平移動で発火
- 既存の矢印ボタンも残す（アクセシビリティ）
- 外部ライブラリは使わず、タッチイベントで実装

### 1-3. ヘッダーのナビゲーション改善

**対象ファイル:** `app/components/calendar/calendar-grid.tsx`

**変更内容:**
- 月表示を大きく: `text-lg font-bold`
- 矢印ボタンのタップ領域拡大: `h-10 w-10` → `h-12 w-12`
- 今月に戻るボタンの追加（「今日」ボタン）

---

## フェーズ2: ボトムシート化とフォーム改善

### 2-1. ダイアログ → ボトムシート

**対象ファイル:**
- `app/components/ui/bottom-sheet.tsx`（新規作成）
- `app/page.tsx`（Dialog → BottomSheet に差し替え）

**変更内容:**
- Radix Dialog を利用しつつ、見た目をボトムシートに変更
- アニメーション: 下からスライドアップ（`translate-y` ベース）
- ドラッグで閉じる機能（上部にドラッグハンドル表示）
- スマホ: 画面高さの90%まで展開
- デスクトップ: 中央モーダルのフォールバック（max-w-lg）

**ボトムシート構造:**
```
┌──────────────────────┐
│      ─── (ハンドル)     │
│ 2025年3月1日         ✕ │
│ ───────────────────── │
│                        │
│  [フォーム/詳細内容]     │
│                        │
│ ───────────────────── │
│  [保存]  [キャンセル]    │
└──────────────────────┘
```

### 2-2. 登録フォームの洗練

**対象ファイル:** `app/components/onigiri/onigiri-dialog.tsx`

**変更内容:**
- セクション区切り: 基本情報 / 写真 / メモ の視覚的グルーピング
- 画像アップロード: タップ可能なカード型プレースホルダー（「＋写真を追加」）
- 星評価: より大きく、タップしやすいサイズに
- 入力フィールド: ラベルをフローティングまたはフィールド上部に統一
- バリデーション: 必須フィールド未入力時のインライン表示

### 2-3. 詳細表示の改善

**対象ファイル:** `app/components/onigiri/onigiri-dialog.tsx`

**変更内容:**
- ヒーロー画像: おにぎり画像をボトムシート上部に大きく表示
- 情報カード: 店舗名・価格・評価をカード風にまとめて表示
- メモ: 全文表示（line-clamp削除）
- 写真比較: おにぎり写真と食べた時の写真を横並び

---

## フェーズ3: 全体の仕上げ

### 3-1. ヘッダー/タブの改善

**対象ファイル:** `app/page.tsx`

**変更内容:**
- アプリタイトルにおにぎりアイコン追加（絵文字: 🍙）
- タブ切り替えをセグメントコントロール風に
- アクティブタブのアニメーション（スライドインジケーター）

### 3-2. 空状態のガイダンス

**対象ファイル:** `app/components/calendar/calendar-grid.tsx`

**変更内容:**
- おにぎり未登録月: カレンダー下部にメッセージ表示
  - 「日付をタップしておにぎりを記録しましょう」
- 検索結果0件: イラスト付きの空状態表示

### 3-3. 保存フィードバック

**対象ファイル:** `app/page.tsx`, `app/components/onigiri/onigiri-dialog.tsx`

**変更内容:**
- `alert()` → トースト通知に変更
- 保存成功時: 「おにぎりを記録しました」のトースト
- ボトムシートが閉じた後にカレンダーセルが更新されるアニメーション

---

## 技術的な制約・注意事項

- **外部ライブラリの追加を最小限に**: スワイプ、ボトムシートは可能な限り自前実装
- **既存の Radix UI を活用**: Dialog の仕組みを流用してボトムシートを構築
- **Tailwind CSS の設計トークンを維持**: `globals.css` のCSS変数体系をそのまま使う
- **段階的に実施**: 各フェーズ完了後に動作確認してから次へ進む
- **既存機能を壊さない**: 保存・読み込み・検索のロジックは変更しない

---

## 実施順序

```
フェーズ1 → 動作確認 → フェーズ2 → 動作確認 → フェーズ3 → 最終確認
```

各フェーズ内でも小さな単位でコミットし、問題があれば即座にロールバック可能にする。
